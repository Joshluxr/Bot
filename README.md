# Bot

Security tooling workspace (Terragon).

## WordPress / CloudFront WAF assessment

See [`tools/wordpress-bruter/README.md`](tools/wordpress-bruter/README.md) for setup, `/wp%2Dlogin.php` bypass testing, and engagement runbook.

```bash
cd tools/wordpress-bruter && ./setup.sh && ./run_tests.sh
```
